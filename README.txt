To update all dependencies:
cd Desktop/Andyscomet/cometd/trunk
mvn versions:use-latest-versions

To clean maven's cache and free some disk space:
mvn clean

Dojox folder contains the latest cometd.js and cometd folder.
Delete them in web-UI/dojox and put these in.
